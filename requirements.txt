# ─────────────────────────────────────────────────────────────────────────────
# MultiSigWallet Projesi — Ana Requirements
# COM 4532 Blockchain Technology and Public Ledgers | Ankara Üniversitesi
#
# Kurulum:  pip install -r requirements.txt
# Python  :  3.9+
# ─────────────────────────────────────────────────────────────────────────────

# ── Blockchain & Web3 ─────────────────────────────────────────────────────────
web3==7.16.0                # Ethereum node bağlantısı, contract çağrıları, event işleme
eth-account==0.13.7         # Private key yönetimi, TX imzalama, EIP-712 desteği
eth-utils==5.3.1            # Adres checksum, Wei dönüşümleri, encoding araçları
eth-typing==5.2.1           # Web3.py tip tanımları (TypedDict'ler)
eth-rlp==2.2.0              # RLP serileştirme (raw TX encoding)
eth-hash==0.7.1             # Keccak256 hash fonksiyonu
hexbytes==1.3.1             # bytes ↔ hex dönüşüm yardımcısı

# ── Ortam & Konfigürasyon ─────────────────────────────────────────────────────
python-dotenv==1.0.0        # .env dosyasından RPC_URL, PRIVATE_KEY, CHAIN_ID okuma

# ── Frontend (İrem — irem/app.py) ─────────────────────────────────────────────
streamlit==1.50.0           # Wallet dashboard UI (login, session, metrikler)
plotly==6.7.0               # Onay durumu pasta grafiği (plotly.express)
pandas==2.3.3               # Pending TX tablosu (DataFrame)
fpdf2==2.8.4                # Dashboard'dan PDF rapor indirme

# ── Rapor Oluşturma ───────────────────────────────────────────────────────────
python-docx==1.2.0          # generate_report.py — Word belgesi (.docx) üretimi

# ── Test (Merve — test/) ──────────────────────────────────────────────────────
pytest==8.4.2               # 33 unit & integration test (Hardhat local node)

# ── Genel Yardımcılar ─────────────────────────────────────────────────────────
requests==2.32.5            # HTTP istekleri (Etherscan API, faucet vb.)
cryptography==46.0.6        # eth-account bağımlılığı — kriptografik operasyonlar
